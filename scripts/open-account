#!/usr/bin/env bash

set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
source "${SCRIPT_DIR}/lib/api.sh"

require_tools

if [[ $# -ne 0 ]]; then
  usage "$(basename "$0")"
fi

api_post "/credit-accounts" "{}"
